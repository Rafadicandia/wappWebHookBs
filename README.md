# wappWebHookBs

This repository is a minimal WhatsApp webhook boilerplate using Express.

Run locally:

```bash
npm install
npm start       # start with Node
npm run dev     # start with nodemon (dev dependency)
```

Create a `.env` with `PORT` and `VERIFY_TOKEN` as needed.